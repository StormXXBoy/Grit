function init()
	print("Test")
	local v = Vector.new(100, 200)
	print(v)
	--player.size = v
	print(tostring(player.size.X))
	print(player.size, player.position)
	--player.size.X = 40;
	--while true do
		print("testin!!")
	--end
end

function  update(dt)
	--print(player.size, player.position)
end

function input(...)
	print(...)
end
